# Decision / Change Log

| Date | Decision / request | Phase | Impact | Status |
| --- | --- | --- | --- | --- |
| 2026-09-20 | Overview site is the app design and UX authority; old site and Done folder are content sources only. | 0 | Controls all visual implementation. | Locked |
| 2026-09-20 | Approved primary journey is Awaken → See Clearly → Become → Join. | 0 | Prevents Walk becoming a fifth stage. | Locked |
| 2026-09-20 | Phase 1 is limited to one end-to-end vertical slice. | 1 | Full curriculum remains unauthorized. | Locked |
| 2026-09-20 | Next.js/React/TypeScript + PostgreSQL/Supabase Auth/RLS + server-side OpenAI accepted. | 1 | Establishes prototype technical baseline. | Locked for Phase 1 |
| 2026-09-20 | Normalized `ai_artifact_sources` and `ai_context_grants` replace JSON ID lists. | 1 | Makes deletion, revocation, RLS, and tests relationally enforceable. | Locked for Phase 1 |
| 2026-09-20 | Hard-delete all application AI artifacts dependent on a deleted source entry. | 1 | Gives deterministic privacy behavior; curriculum state remains. | Locked for Phase 1 |
| 2026-09-20 | Canonical prototype palette follows Overview/Formation/Awaken values; later-page token drift is not adopted. | 1 | Freezes prototype token direction. | Approved |
| 2026-09-20 | Phase 1 does not persist full AI transcripts; explicitly saved AI material uses normalized artifact sources. | 1 | Makes deletion dependencies deterministic and minimizes retained sensitive text. | Locked for Phase 1 |
| 2026-09-20 | Same-owner child relationships use composite foreign keys; cross-stage links use typed columns. | 1 | Enforces ownership integrity below the service layer. | Locked for Phase 1 |
| 2026-09-20 | Practice transitions use a locked, versioned database function; reopen/duplicate is deferred. | 1 | Prevents invalid or concurrent lifecycle changes. | Locked for Phase 1 |
| 2026-09-20 | `shared-assets.json` is an obsolete machine-local input, not a missing canonical source file. It will not be recreated; the preview harness will be made self-contained against existing `src/assets` under test. | 1 | Preserves the Overview source while making visual authority reproducible. Visual baselines remain blocked until the repair passes. | Approved planning resolution |
| 2026-09-20 | Detailed TDD plan, bounded agent work packages, test matrix, exact seed inventory, and definition of done established before implementation. | 1 | Fixes implementation order, ownership, gates, and completion evidence. | Awaiting implementation-checkpoint approval |
| 2026-09-20 | Task 0 preview repair removes only the obsolete external manifest dependency and serves the unchanged `src` authority through the existing explicit allowlist. | 1 | Restores deterministic design-authority testing without re-baselining or modifying Overview content. | Implemented and verified |
| 2026-09-20 | Task 1 harness uses real local Supabase roles for integration/pgTAP tests and a deterministic in-process fake for ordinary AI tests; destructive commands fail closed outside loopback test infrastructure. | 1 | Prevents mocks from substituting for RLS proof and prevents ordinary tests from reaching live OpenAI or hosted databases. | Implemented; runtime verification blocked by R-018 |
| 2026-09-21 | Task 1 certification disables Playwright server reuse, adds bounded concurrency-barrier diagnostics, and uses a rollback-only RLS probe table in pgTAP. | 1 | Prevents false browser passes against an unrelated server and proves the harness can exercise authenticated A/B row isolation without adding application schema. | Implemented; runtime execution blocked by R-018 |
| 2026-09-21 | Task 3 privileged functions resolve the actor through a parameterless `rts_private.current_actor()` function that reads the verified PostgREST JWT claim settings, preferring `request.jwt.claims.sub` with legacy `request.jwt.claim.sub` fallback only when current claims are absent. Privileged functions also reside in the non-exposed `rts_private` schema. | 1 | Removes fragile custom privileges on Supabase-managed `auth` objects while preserving the authenticated session/JWT identity boundary, fixed function search paths, non-login ownership, RLS, and same-owner enforcement. PostgreSQL 17 necessarily records an ADMIN-only creator edge for the local `postgres` migration actor with bootstrap grantor `supabase_admin`; tests require this exact edge, `INHERIT false`, `SET false`, owner `NOLOGIN`, no other members, and no non-owner schema `CREATE` grant. | Approved narrow implementation adjustment |
| 2026-09-21 | Add `rts_private.save_awaken_observation(text,text,text)` as the single atomic Task 6 save boundary. | 1 | Writes the three exact Awaken entries and their approved user-authored structured records, then advances only the independent curriculum resume pointer; it accepts no caller user ID and creates no practice, score, or evidence field. | Approved narrow integrity extension |
| 2026-09-21 | Add `pending` to `ai_outcome` solely for atomic owner-scoped AI dispatch reservation. | 1 | Prevents concurrent duplicate provider calls without storing prompts/responses. Matching terminal retries return content-free `already_completed`; conflicting fingerprints are rejected. A two-minute abandoned lease terminalizes as existing `provider_error` without redispatch, and reservation shares the actor/curriculum lock with Reflect progress mutation. | Approved narrow Task 7 adjustment |
