'use client';
import { FormEvent,useState } from 'react'; import { Button } from '../design-system/Button'; import { Field } from '../design-system/Field';
type Result={ok:boolean;conflict?:boolean};
export function PracticeReview({practiceId,expectedLockVersion,submit}:{practiceId:string;expectedLockVersion:number;submit(v:{practiceId:string;expectedLockVersion:number;reviewText:string}):Promise<Result>}){const [text,setText]=useState('');const[conflict,setConflict]=useState(false);async function onSubmit(e:FormEvent){e.preventDefault();const r=await submit({practiceId,expectedLockVersion,reviewText:text});setConflict(Boolean(r.conflict));}return <form onSubmit={onSubmit}><Field id="practice-review" label="What are you noticing now?" value={text} onChange={e=>setText(e.target.value)} required />{conflict?<p role="alert">This practice changed in another session. Your words are still here; refresh before trying again.</p>:null}<Button type="submit">Save review</Button></form>}
