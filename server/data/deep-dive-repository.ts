import pg from 'pg';
import type { A1SectionId, DeepDiveProgress } from '../../domain/deep-dive';

type Input = Readonly<{ actorId: string }>;
let pool: pg.Pool | undefined;
function databasePool() { const url = process.env.RTS_DATABASE_URL ?? process.env.DATABASE_URL ?? (process.env.RTS_TEST_MODE === '1' ? process.env.TEST_DATABASE_URL : undefined); if (!url) throw new Error('RTS_DATABASE_URL or DATABASE_URL is required for Deep Dive persistence.'); return pool ??= new pg.Pool({ connectionString: url }); }
async function authenticated<T>(actorId: string, run: (client: pg.PoolClient) => Promise<T>) { const client = await databasePool().connect(); try { await client.query('begin'); await client.query('set local role authenticated'); await client.query("select set_config('request.jwt.claims', $1, true)", [JSON.stringify({ sub: actorId })]); await client.query("select set_config('request.jwt.claim.sub', $1, true)", [actorId]); const result = await run(client); await client.query('commit'); return result; } catch (e) { await client.query('rollback'); throw e; } finally { client.release(); } }

export interface DeepDiveRepository { get(actorId: string): Promise<DeepDiveProgress | null>; saveSection(input: Input & { sectionId: A1SectionId }): Promise<void>; saveReflection(input: Input & { body: string }): Promise<void>; deleteReflection(input: Input): Promise<void>; complete(input: Input): Promise<void>; }
export function deepDiveRepository(): DeepDiveRepository { return { get: actorId => authenticated(actorId, async c => { const row = (await c.query(`select p.id,p.last_section_id,p.completed_at,r.body from public.deep_dive_module_progress p left join public.deep_dive_reflections r on (r.progress_id,r.user_id)=(p.id,p.user_id) and r.prompt_id='real-moment' where p.user_id=$1 and p.module_id='awaken.pay-attention'`, [actorId])).rows[0]; return row ? { id: row.id, lastSectionId: row.last_section_id, completedAt: row.completed_at, reflection: row.body ?? null } : null; }), saveSection: input => authenticated(input.actorId, async c => { await c.query(`insert into public.deep_dive_module_progress(user_id,curriculum_version_id,module_id,last_section_id) values($1,'phase-1-v1','awaken.pay-attention',$2) on conflict(user_id,curriculum_version_id,module_id) do update set last_section_id=excluded.last_section_id,updated_at=now()`, [input.actorId, input.sectionId]); }), saveReflection: input => authenticated(input.actorId, async c => { await c.query(`insert into public.deep_dive_module_progress(user_id,curriculum_version_id,module_id,last_section_id) values($1,'phase-1-v1','awaken.pay-attention','reflection') on conflict(user_id,curriculum_version_id,module_id) do update set updated_at=now()`, [input.actorId]); await c.query(`insert into public.deep_dive_reflections(user_id,progress_id,prompt_id,body) select $1,id,'real-moment',$2 from public.deep_dive_module_progress where user_id=$1 and module_id='awaken.pay-attention' on conflict(progress_id,prompt_id,user_id) do update set body=excluded.body,updated_at=now()`, [input.actorId, input.body]); }), deleteReflection: input => authenticated(input.actorId, async c => { await c.query(`delete from public.deep_dive_reflections where user_id=$1 and prompt_id='real-moment'`, [input.actorId]); }), complete: input => authenticated(input.actorId, async c => { await c.query(`insert into public.deep_dive_module_progress(user_id,curriculum_version_id,module_id,last_section_id,completed_at) values($1,'phase-1-v1','awaken.pay-attention','carry-forward',now()) on conflict(user_id,curriculum_version_id,module_id) do update set last_section_id='carry-forward',completed_at=coalesce(public.deep_dive_module_progress.completed_at,now()),updated_at=now()`, [input.actorId]); }) }; }
